import { Center, Container, Text } from "native-base";

const ShowsContainer = ({ navigation }) => {
  return (
    <Container>
      <Center>
        <Text>Shows Container</Text>
      </Center>
    </Container>
  );
};

export default ShowsContainer;
